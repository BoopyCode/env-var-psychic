# ENV Var Psychic 🔮

Prevents runtime environment variable failures by checking your .env file before deployment. The psychic sees your missing variables before they see you! Run: `node env-psychic.js`